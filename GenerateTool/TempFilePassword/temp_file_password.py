import os
"""
def temp_file_password(password):
    with open("temp.key", "w") as temp:
        temp.write(f"{password}\n")

if __name__ == '__main__':
    pa = "browhatsup"
    temp_file_password(pa)

"""